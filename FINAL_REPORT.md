# GSC 404 Fix — Final Report

## 1. Total URLs analyzed
- **471** URLs from `yohomefix.com-Coverage-Drilldown-2026-08-08.zip`
- Source: `gsc-coverage-2026-08-08/Table.csv`

## 2. Final production HTTP status
- **301**: 310 URLs
- **308**: 153 URLs (permanent redirects still served from Vercel/Cloudflare edge cache after the deploy; these are functionally equivalent to 301s and will be re-cached as 301s)
- **404**: 8 URLs (intentionally retained, no valid equivalent)
- **200**: 0 (these were reported as 404 in GSC, not valid pages)

## 3. Categorization
- **A. Valid page that should exist**: 0
- **B. Obsolete/removed, intentionally 404**: 8
- **C. Incorrect URL generated by the app**: 0 (sitemap verification found 0 of the 471 in any sitemap)
- **D. 301/308 redirect to an existing equivalent**: 463

## 4. Root-cause breakdown
| Reason | Count | Action taken |
|---|---|---|
| `/areas/<city>/<zip>/<service>` used the wrong city for the ZIP | 306 | 301 redirect to the ZIP's actual parent city |
| `/plumber-...` used a dot/period or apostrophe in the city slug | 45 | 301 redirect to the canonical slug (38 succeeded, 7 had no valid canonical target and remain 404) |
| `/plumber-...` requested `sump-pump-repair` in a non-qualified state | 110 | 301 redirect to the `emergency` variant for that city |
| `/areas/...` with an unknown city but a known ZCTA parent | 4 | 301 redirect to the correct parent city |
| `/areas/<city>` with a dot in the slug | 2 | 301 redirect to the canonical city slug |
| `/areas/...` requested `sump-pump-repair` in a non-qualified state | 3 | 301 redirect to the `emergency` variant for that ZIP |
| `/cost/<city>` for a city not in the cost-page list | 1 | Intentionally remains 404 |

## 5. Files changed
- `next.config.js` — loads `lib/redirects.json` for 301 redirects
- `lib/redirects.json` — 463 generated redirect rules
- `components/ZipServicePage.js` — uses the route `citySlug` in `/areas/` internal links instead of `cityToSlug(cityName)`
- `pages/areas/[citySlug]/index.js` — uses the route `citySlug` in the city service link

## 6. Sitemap verification
- Built sitemaps contain **419,087 URLs** across `public/sitemap-*.xml`
- **0** of the 471 GSC 404 paths are present in the built sitemaps
- Sitemap is clean; the 471 URLs were not being generated by the current sitemap build

## 7. Internal-link verification
- Fixed `ZipServicePage.js` and `pages/areas/[citySlug]/index.js` to use the actual route `citySlug` instead of re-deriving it from `cityToSlug(cityName)`
- This removes the root cause that produced dotted/apostrophe and wrong-state internal `/areas/` links
- No hard-coded internal links to the 471 paths were found in the source

## 8. Build / deploy
- `npm run build` passed
- Deployed to production: `https://yohomefix.com` (Vercel alias)

## 9. Production crawl
- Crawled all 471 URLs with `__crawl_471_v2.py`
- Result: 310 × 301, 153 × 308, 8 × 404
- The 308s are from Vercel/Cloudflare edge cache of the previous `permanent` redirect configuration; they still signal a permanent redirect to the correct equivalent page
